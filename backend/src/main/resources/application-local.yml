spring:
  datasource:
    url: jdbc:h2:file:./data/local/sufe-ai;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
server:
  address: 127.0.0.1
  servlet:
    session:
      cookie:
        secure: false
sufe:
  bootstrap:
    demo-data-enabled: true
    demo-password: ${SUFE_DEMO_PASSWORD}
