# SUFE AI Demo 交接运行说明

## 项目目录

进入正式项目目录：

```powershell
Set-Location -LiteralPath 'D:\桌面\上财'
```

## 第一次运行

另一台电脑需要先安装 Node.js，然后在项目目录执行：

```powershell
npm install
npm run dev -- --host 0.0.0.0 --port 5174
```

本机打开：

```text
http://localhost:5174/
```

同一 Wi-Fi 下其他设备访问时，使用终端里显示的 Network 地址。

## 快速启动

也可以双击：

```text
启动演示服务.bat
```

## 打包验证

```powershell
npm run lint
npm run build
```

## 注意

- 不需要复制 `node_modules`，另一台电脑执行 `npm install` 会重新安装依赖。
- `public/demo-assets` 里包含演示用 PPT、视频和 PPT 预览图，已经一起打包。
- 当前 Demo 数据存在浏览器 `localStorage`，换电脑后会是新的本地演示数据。
