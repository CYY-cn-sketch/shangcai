# 上财 AI 创业实践教学平台交接运行说明

## 本地运行

启动后端：

```powershell
Set-Location -LiteralPath 'D:\桌面\shangcai\backend'
.\mvnw.cmd package
powershell -ExecutionPolicy Bypass -File .\start-mysql-local.ps1
```

启动前端：

```powershell
Set-Location -LiteralPath 'D:\桌面\shangcai'
npm install
npm run dev -- --host 0.0.0.0 --port 5174
```

- 前端：`http://localhost:5174/`
- 后端健康检查：`http://127.0.0.1:8080/actuator/health`

## 打包验证

```powershell
npm run test:ci

# 需要在当前进程提供专用测试密码；不要写入仓库
$env:SUFE_E2E_PASSWORD = '<专用测试密码>'
npm run test:e2e
Remove-Item Env:SUFE_E2E_PASSWORD

Set-Location -LiteralPath 'D:\桌面\shangcai\backend'
.\mvnw.cmd test
```

当前质量门禁包括 ESLint、6 项 Vitest 单元测试、最低覆盖率基线、生产构建、74 项后端测试，以及学生/教师在桌面端与移动端的 4 条 Playwright 流程。`.github/workflows/quality.yml` 会在 GitHub 推送和拉取请求时使用隔离 H2 数据库重复执行这些检查，且保持 WorkBuddy、乐享关闭。

## 生产拓扑

单台云服务器一期采用以下结构：

```text
浏览器 HTTPS
  -> Nginx 443
     -> /          /var/www/sufe-ai 前端静态文件
     -> /api/*     127.0.0.1:8080 Spring Boot
     -> /healthz   Spring Boot readiness

Spring Boot
  -> 127.0.0.1:3306 MySQL
  -> /srv/sufe-ai/data/files
  -> /srv/sufe-ai/data/artifacts
  -> /srv/sufe-ai/data/workbuddy/jobs
  -> /var/log/sufe-ai

备份
  -> /srv/sufe-ai/backups/platform
     -> MySQL 压缩备份
     -> files、artifacts、workbuddy/jobs 文件归档
     -> SHA-256 校验清单
```

Java 与 MySQL 不直接暴露公网端口，正式环境必须启用 HTTPS。

## 生产部署准备

1. 安装 Nginx、Java 21、MySQL 8.4、Node.js 22 和 `mysqldump`。
2. 创建系统用户 `sufe-ai`，创建并授权以下目录：

```text
/opt/sufe-ai/backend
/var/www/sufe-ai
/etc/sufe-ai
/srv/sufe-ai/data/files
/srv/sufe-ai/data/artifacts
/srv/sufe-ai/data/workbuddy/jobs
/srv/sufe-ai/backups/platform
/var/log/sufe-ai
```

3. 复制 `deploy/env/backend.env.example` 到 `/etc/sufe-ai/backend.env`，填写真实数据库密码并设置权限 `600`。
4. 复制 `deploy/mysql/mysql-backup.cnf.example` 到 `/etc/sufe-ai/mysql-backup.cnf`，填写只具备备份权限的 MySQL 账号；该文件需由 `sufe-ai` 用户读取，可执行 `chown sufe-ai:sufe-ai /etc/sufe-ai/mysql-backup.cnf && chmod 600 /etc/sufe-ai/mysql-backup.cnf`。
5. 构建并上传：

```powershell
npm ci
npm run test:ci

Set-Location -LiteralPath '.\backend'
.\mvnw.cmd test
.\mvnw.cmd -DskipTests package
```

6. 将 `dist` 内容发布到 `/var/www/sufe-ai`，将后端 JAR 发布为 `/opt/sufe-ai/backend/sufe-ai-backend.jar`。
7. 确认 `deploy/scripts/*.sh` 可执行，安装 `deploy/systemd/` 下的后端服务和备份定时器；安装前先核对目录、用户和环境文件。
8. 替换 Nginx 模板中的域名和证书路径，执行 `nginx -t` 后再 reload。
9. 启动服务后执行：

```bash
BASE_URL=https://正式域名 /opt/sufe-ai/deploy/scripts/smoke-test.sh
```

## 正式服务器参数记录

以下信息目前均待项目方提供，填写完成后再执行云端部署：

| 参数 | 当前状态 |
| --- | --- |
| 云服务器公网 IP / SSH 地址 | 待提供 |
| Linux 发行版与版本 | 待提供 |
| CPU / 内存 / 系统盘 / 数据盘 | 待提供 |
| 正式域名 | 待提供 |
| HTTPS 证书及私钥存放方式 | 待提供；不要提交到 Git |
| SSH 管理账号与授权方式 | 待提供；优先使用密钥登录 |
| MySQL 正式实例与最小权限账号 | 待提供 |
| 文件数据与备份目标目录 | 待提供 |
| 上线窗口 / 回滚窗口 | 待确认 |

## 日志、备份与回滚

- Java 日志默认写入 `/var/log/sufe-ai/backend.log`，按大小和日期滚动，保留 30 天且总量不超过 2 GB。
- 平台每天 02:30 同时备份 MySQL 与 `/srv/sufe-ai/data`，每组备份包含数据库压缩包、文件归档和 SHA-256 校验清单，默认保留 14 天。
- 首次上线先在隔离目录执行 `sha256sum -c <校验清单>`，再把数据库恢复到空库、把文件归档解压到空目录；核对账号、知识资料下载和成果下载后，才能把该备份方案视为可用。
- 发布时保留上一个 JAR 和上一份前端目录。回滚先恢复前端/JAR，再重启后端并执行冒烟测试。
- Flyway 迁移采用向前兼容方式；不要手工修改已经执行的迁移文件。

## 上线前必须确认

- 正式域名、HTTPS 证书和服务器操作系统。
- CPU、内存、磁盘容量及文件备份目标。
- MySQL 正式账号与最小权限。
- WorkBuddy Worker 数量、供应商额度和结果目录。
- 乐享企业账号 API 并发/QPS、应用凭据和知识空间 ID。

WorkBuddy 和乐享在模板中默认关闭；未取得明确确认时不要改为 `true`。
