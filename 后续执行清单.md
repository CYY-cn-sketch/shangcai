# Day 25–30 执行与交付清单

## 当前基线（2026-07-17）

- 正式项目路径：`D:\桌面\shangcai`
- 前端：`http://localhost:5174/`，当前返回 HTTP 200
- 后端：`http://127.0.0.1:8080/`，当前健康状态为 `UP`
- MySQL：`SUFE_MySQL84`，当前 `Running / Automatic`
- 数据库迁移：Flyway V1–V13
- 后端控制器覆盖认证、账号权限、审计、知识库、专家 Skill、学生工作区、答辩、成果、教师审核、生成任务和供应商网关
- 浏览器业务数据已退出 `localStorage` / `sessionStorage`，由 Java 后端、MySQL 和受控文件目录持久化
- WorkBuddy、乐享真实调用保持关闭，本阶段未产生供应商费用

## Day 25：全量回归

- [x] 后端全量测试通过：74 项，0 失败、0 错误
- [x] 前端 `npm run lint` 通过
- [x] 前端 6 项 Vitest 单元测试和最低覆盖率基线通过
- [x] 前端 `npm run build` 通过
- [x] 学生/教师在桌面端与移动端的 4 条 Playwright 流程通过
- [x] GitHub Actions 质量门禁已覆盖前端检查、后端测试和隔离 H2 浏览器验收
- [x] 管理员真实登录、退出和会话恢复链路验证
- [x] 桌面端页面无横向溢出、无未标注按钮、无缺失图片替代文本
- [x] 320、375、768 像素宽度移动端复测，无顶栏重叠和横向溢出
- [x] 浏览器运行时异常和 HTTP 失败均为 0
- [x] 退出接口返回 204，退出后回到登录页

## Day 26：安全与数据边界复核

- [x] 业务状态不再写入浏览器本地存储
- [x] 本地凭据文件受 Git 忽略，不进入提交
- [x] 生产依赖使用 npm 官方源审计，发现 0 个漏洞
- [x] 代码与部署模板未发现私钥或明文密钥
- [x] 生产 Session Cookie 设置为 `Secure`、`HttpOnly` 和 `SameSite=Strict`
- [x] 生产环境默认关闭演示数据和两个供应商真实执行器
- [x] Nginx 模板包含 HTTPS、限流、安全响应头和 CSP 报告模式
- [ ] 上线前根据正式域名观察 CSP 报告，再决定切换为强制模式

## Day 27：部署准备

- [x] 增加 Spring Boot `production` Profile
- [x] Java 仅监听 `127.0.0.1:8080`，由 Nginx 对外提供 HTTPS
- [x] 增加生产环境变量示例，不包含真实密码或密钥
- [x] 增加 Nginx、systemd 后端服务、平台数据备份服务与定时器模板
- [x] 固定生产目录：程序、数据、日志、备份分离
- [x] 增加部署后冒烟检查脚本，以及覆盖 MySQL、知识原文件和成果文件的平台备份脚本
- [x] 补齐运行、日志、备份、回滚和故障定位说明
- [ ] 服务器到位后在 Linux 上执行 `nginx -t` 和 systemd 原生校验
- [ ] 在正式服务器完成一次真实备份与恢复演练

## Day 28：交付材料同步

- [x] README 更新为正式项目运行与验证说明
- [x] 交接运行说明更新为当前部署拓扑和操作步骤
- [x] 本清单更新为 Day 25–30 的事实状态
- [x] 同步内部与外部两份 30 天 Word 计划的实际进度，并完成结构校验
- [ ] 补充版逐页 PNG 版式检查（本机缺少 LibreOffice，当前无法执行）
- [x] 在交接运行说明中形成正式服务器参数记录区
- [ ] 项目方填写正式域名、系统版本、CPU、内存、磁盘和 SSH 管理方式

## Day 29：预发布与用户验收

- [ ] 在项目方购买的正式服务器完成 Nginx、Java、MySQL 和目录部署
- [ ] 配置正式域名、HTTPS 证书、数据库账户和备份凭据
- [ ] 以学生、教师、管理员三种角色执行预发布验收
- [ ] 完成大文件上传、成果下载和审核退回验证
- [x] 完成账号禁用、账号删除与已有会话立即失效的自动化验证
- [ ] 验证日志轮转、磁盘容量告警、备份保留与恢复

阻塞条件：正式服务器、域名和证书尚未提供。

## Day 30：上线与培训

- [ ] 项目方确认上线窗口和回滚窗口
- [ ] 执行上线前数据库备份并记录版本号
- [ ] 发布前端静态资源和后端 JAR，执行冒烟测试
- [ ] 完成管理员、教师操作培训与交接签收
- [ ] 观察上线后的登录、错误率、任务队列、磁盘和备份状态

阻塞条件：Day 29 预发布通过后才能执行。

## 供应商接入边界

- WorkBuddy 与乐享可以完成代码接入、契约校验和禁用状态测试。
- 未经用户明确确认，不启用真实执行器，不调用计费接口，不消耗账号积分或供应商额度。
- 正式启用前仍需供应商确认 API 权限、并发/QPS、失败重试和费用规则。

## 当前已知非阻塞项

- `App.tsx` 已降至 499,421 字节；主业务包约 484.63 kB，Three.js 独立包约 523.98 kB，当前生产构建不再产生分包警告，后续仍按小步方式拆分。
- Flyway 对 MySQL 8.4 仍可能提示版本高于已测试范围，但 V1–V13 已在本地实际迁移并运行。
